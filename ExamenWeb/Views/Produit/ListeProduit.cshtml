@model IEnumerable<Domain.Entities.Produit>


@*@{
    ViewBag.Title = "ListeProduit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}*@

<h2>ListeProduit</h2>
@using (Html.BeginForm())
{
    <table>
        <tr>
            <td>
                <div> Serach by name : @Html.TextBox("SearchString")    </div>
            </td>

            <td>
                <input type="submit" value="Search" id="submit" />
            </td>
        </tr>
    </table>
}



@*<div class="container">
    <div class="form-group">
        <div class="input-group">
            <span class="input-group-addon"><i class="glyphicon glyphicon-search"></i></span>
            <input type="text" id="Search" placeholder="Search" class="form-control"/>

        </div>

    </div>
</div>*@


<a class="logout">
    @Html.ActionLink("Create New", "Create")
</a>

<table class="table table-striped table-advance table-hover">

    <tr>
        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Categorie.Nom)
        </th>

        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Boutique.Nom)
        </th>

        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Libelle)
        </th>
        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Nom)
        </th>

        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Prix)$
        </th>

        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Quantitee)
        </th>
        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th href="basic_table.html#">
            @Html.DisplayNameFor(model => model.Image)
        </th>


        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr class="Search">
        <td>
            @Html.DisplayFor(model => item.Categorie.Nom)
        </td>

        <td>
            @Html.DisplayFor(model => item.Boutique.Nom)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Libelle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Prix)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Quantitee)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @{string path = "~/Content/Upload/" + item.Image;}

            <img src="@Url.Content(path)" width="50" height="50" />
        </td>



        <td>


            <button class="btn btn-success btn-xs">
                @Html.ActionLink("_", "Edit", new { id = item.Id_produit })
                <i class="fa fa-check"></i>
            </button>

            <button class="btn btn-primary btn-xs">
                @Html.ActionLink("_", "Details", new { id = item.Id_produit })
                <i class="fa fa-pencil"></i>
            </button>

            <button class="btn btn-danger btn-xs">
                @Html.ActionLink("_", "Delete", new { id = item.Id_produit })
                <i class="fa fa-trash-o "></i>
            </button>

</td>
    </tr>
}

</table>

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script>
    $(document).ready(function () {
        function Contains(test_one, text_two) {
            if (text_one.indexOF(text_two) != -1)
                return true;
        }
        $("#Search").keyup(function () {
            var searchText = $("#Search").val().toLocaleLowerCase();
            $(".Search").each(function () {
                if (!Contains($(this).text().toLocaleLowerCase(), searchText)) {
                    $(this).hide();
                }
                else {
                    $(this).show();
                }
            });
        });
    });


</script>
